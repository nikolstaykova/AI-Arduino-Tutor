# Part media

Images referenced by `library/parts/*.json`'s `image` field go here, named to match (e.g. `led.png`, `resistor-220.png`).

Not yet populated — filenames are already reserved in each part's card so the schema doesn't need to change once they exist.
